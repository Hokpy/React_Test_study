# Создать файл docker-compose.yml в корне проекта:
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: react_study_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: rootpass123
      MYSQL_DATABASE: name_db
      MYSQL_USER: user_name
      MYSQL_PASSWORD: Pass
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
